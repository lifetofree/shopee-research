---
name: Shopee Affiliate TH data via local web app
labels: [wayfinder:map]
status: open
created: 2026-07-16
tracker: local-markdown
---

# Shopee Affiliate TH data via local web app

## Destination

A local FastAPI web app (Python) on `localhost`, single-page HTML/JS UI, authenticating to Shopee Affiliate Thailand via a user-supplied session cookie stored in `.env`. The user types a product name into the UI; the app searches the affiliate portal and renders matching products (image, commission, price, sold). The user can select items and save them to a persistent SQLite store, and per saved item generate a Thai-language caption + English-hashtag summary (≤250 chars total) plus an English 8-second clip prompt. This map ships those generation buttons backed by **templated stubs** behind a clean interface ready to swap in an LLM later.

## Notes

- **Tracker**: local-markdown (`.wayfinder/` folder, no remote issue tracker configured).
- **Domain**: Web scraping / affiliate data extraction / personal tooling.
- **Stack pinned**: Python, FastAPI, SQLite (SQLAlchemy 2.x), Playwright (Python — for cookie helper, possibly fallback transport), httpx for HTTP, uv for env management, uvicorn to run, pydantic-settings for config.
- **Conventions**:
  - Don't use the Shopee Open API (out of scope by definition — destination explicitly says "without Shopee open API").
  - Auth = single replayed session cookie via `.env`. No headless login flow beyond the helper script.
  - Localhost only. No auth on the web UI itself (per D1).
  - Every `wayfinder:*` session treats a single ticket's body as the unit of work and does not exceed it.
- **Skills to consult while resolving tickets**: web-search, prototype (for any throwaway UI), grilling / domain-modeling when scope flickers.

## Decisions so far

<!-- index — one line per closed ticket. Append as tickets close. -->

- [Research — Map the available Shopee Affiliate TH data surfaces](.wayfinder/tickets/research-map-data-surfaces.md) — `shopee.co.th/api/v4/search/search_items` is the primary surface (image/price/sold via `http`), with mandatory `af-ac-enc-dat: null` + `x-api-source: pc` headers and price divisor 100000; commission must come from `affiliate.shopee.co.th` (Surface B), whose JSON contract is still empirical — captured by the `capture-affiliate-portal-traffic` ticket, written up at [`docs/research/data-surfaces.md`](../../docs/research/data-surfaces.md).
- [Bootstrap — Scaffold the Python project](.wayfinder/tickets/bootstrap-python-project.md) — `uv` + hatchling + `pyproject.toml`; FastAPI/uvicorn/SQLAlchemy[asyncio]/aiosqlite/pydantic/pydantic-settings/python-dotenv/httpx/playwright as runtime; pytest/pytest-asyncio under `[project.optional-dependencies].dev`. `src/shopee_th/main.py:create_app()` factory + module-level `app`; `/` and `/health` return `StatusResponse(status, version)`. `Settings` (pydantic-settings) is all-optional with safe defaults. `Makefile` wires `install`, `run`, `test`, `lint`, `e2e` (env-gated skip-with-message), `smoke`, `dev-reset` (with prompt), and `refresh-cookie`/`capture-affiliate-traffic` as TODO stubs. Pytest 2/2 green. `unblocks` all four downstream tickets that depend on a runnable project.
- [Implement search service module](.wayfinder/tickets/implement-search-service.md) — `services.search:search()` + `SearchError(url, code, guidance)`; Surface A wired with the documented headers/params/field extraction (`cf.shopee.co.th/file/` image prefix, micro / 100_000 price, `historical_sold`); Surface B scaffolded best-effort (placeholder URL + GraphQL body, capture ticket owns the contract); `Transport` Protocol with `HttpTransport` (10s timeout) and `NoopTransport` (scripted queue + raise-on-call); single-retry linear-backoff (0.5s) for transient errors; `error: 90309999` raises immediately with refresh-cookie guidance. Library-importable: no FastAPI / ORM / app-config imports in `services.search`. `Item` reconciled with the persistence layer's shape (`title: str` promoted, `price: float`). 17 search tests + 14 repository tests + 2 health tests, all green (33/33). `unblocks` `implement-fastapi-http-layer` and `capture-affiliate-portal-traffic`.
- [Implement caption + clip-prompt templated stubs](.wayfinder/tickets/implement-output-templates.md) — `services.generation.OutputGenerator` Protocol (`@runtime_checkable`) with `caption(item)` and `clip_prompt(item)`; `TemplateGenerator` (default, env `stub`) produces a Thai body (`🔥 {title} | [แบรนด์ {brand} |] ราคา ฿{price} | ขายแล้ว {sold} ชิ้น | ส่งฟรี #ShopeeTH`, body ≤180 chars, total ≤250 with iterative shrink-then-drop-hashtag truncation) plus 4-7 English hashtags (`#ShopeeTH` + category + price-band + 2 from title); `clip_prompt` is a ≤300-char English 8-second brief. `LLMGenerator` skeleton raises `NotImplementedError` on every call (explicit slot for the follow-up map). Factory `get_generator(name=None)` reads `SHOPEE_TH_GENERATOR` from env (default `"stub"`); unknown env values fall back to stub. 30 tests cover all the ticket's listed scenarios (length cap across 7 seed inputs, hashtag count, empty title fallback, missing brand/category, English-only clip prompt, LLM raises, factory wiring). 63/63 total. `unblocks` `implement-fastapi-http-layer`.
- [Implement FastAPI HTTP layer](.wayfinder/tickets/implement-fastapi-http-layer.md) — `api.routes` registers the 7 documented routes under `/api` (`POST /api/search`, `GET|POST /api/saved`, `DELETE /api/saved/{id}`, `POST /api/saved/{id}/caption|clip-prompt`, `GET /api/saved/{id}/outputs?kind=…`) + `/health`. `main.create_app(settings=, transport=, generator=)` is the composition root (all collaborators overridable for tests); `lifespan` runs `init_db()` on startup + closes the transport on shutdown; `CORSMiddleware` uses `allow_origin_regex=r"http://localhost(:\d+)?"` so any localhost port works; static-files mount at `/` serves `static/index.html` (placeholder until the SPA lands); uniform `ApiError` body via `HTTPException(detail=ApiError(...).model_dump())` with stable `error` codes (`search_failed`, `not_found`, `invalid_input`); `SearchError` → 502 with `url`/`code`/`guidance` propagated. 12 e2e tests cover the full lifecycle + 11 focused edge cases; 76/76 total. `unblocks` `build-single-page-html-js-frontend` and `dev-runbook-and-smoke-test`.
- [Build single-page HTML/JS frontend](.wayfinder/tickets/build-single-page-html-js-frontend.md) — `static/{index.html, app.js, styles.css}` replaced the FastAPI ticket's placeholder. Vanilla JS, no framework, no build step. IIFE module pattern (`window.ShopeeTH` is the only public surface for the test smoke hook). Layered: constants → state → API → format → render → handlers → init. `<template>` clones for cards / saved items / output rows (no `innerHTML`); `<details>`-collapsed output history loaded async on render; new generations *prepended* so newest is on top. `state.saving` (Set of source_ids) gates double-clicks at the UI layer; the server's UNIQUE on `source_id` is the load-bearing guarantee. `state.inflight` (Set of saved-item ids) gates caption/clip/remove on a saved item. Uniform `request()` wrapper with 15 s timeout; `navigator.clipboard.writeText` with `execCommand("copy")` fallback. `tests/test_ui.py` covers 10 static-smoke invariants (HTML shell, asset references, all 7 documented endpoints, IIFE/strict, no Tailwind, no login UI, error-banner rendering, idempotency guard). 86/86 total. `unblocks` `dev-runbook-and-smoke-test`.
- [Spec — Synthesize map + tickets into a single spec](.wayfinder/spec.md) — `ready-for-agent`. One FastAPI HTTP API seam (search → save → list → caption → outputs → clip-prompt → delete via `httpx.AsyncClient(app=app)`); lower seams for unit-level coverage; `docs/research/affiliate-observed-traffic.json` is the empirical contract test target for the Surface B leg.
- [Implement SQLite persistence](.wayfinder/tickets/implement-sqlite-persistence.md) — `src/shopee_th/models/db.py` (`SavedItem` + `Output` ORM, async engine/session factory, `init_db` via `create_all`, per-connection `PRAGMA foreign_keys=ON` so SQLite actually enforces FK + cascade) + `models/repository.py` (`save_item` idempotent on `source_id` — first save wins, payload never overwritten; `list_saved` newest-first; `get_saved`; `delete_saved` returns bool + cascades to outputs; `add_output` history-preserving; `list_outputs` newest-first, optional kind filter) + `SavedItemDTO`/`OutputDTO` Pydantic shapes separate from ORM. 14/14 new integration tests green (round-trip, idempotency, cascade, UNIQUE + FK constraints). `unblocks` `implement-fastapi-http-layer`.
- [Fixes from /review of the full repo](.wayfinder/tickets/implement-search-service.md) — after `main` was fast-forwarded to include the persistence/search/templates/FastAPI/frontend branch (and the orphaned, superseded `feat/output-templates` branch was deleted), a two-axis review surfaced and this session fixed three issues: `models/domain.py` had started importing the ORM module at runtime (broke the documented DTO/ORM separation and the "library-importable" guarantee `search.py`/`generation.py` claim — fixed via `TYPE_CHECKING`-only import); `create_app()` called `get_generator()` with no args, which reads `os.environ` directly, while pydantic-settings loads the generator setting from the env file without touching `os.environ` — so the documented `SHOPEE_TH_GENERATOR` switch silently did nothing (fixed by passing `settings.generator` through); `TemplateGenerator.caption()` could shrink hashtags below `HASHTAG_MIN` on long title/brand input while still satisfying the 250-char cap (fixed with a per-hashtag length cap + corrected shrink order). 3 regression tests added, 88/88 green.
- [Implement Playwright cookie refresh helper](.wayfinder/tickets/implement-cookie-refresh-helper.md) — `scripts/refresh_cookie.py`: single headed Chromium context visits `shopee.co.th` then `affiliate.shopee.co.th`, blocks on an Enter keypress after each (guarded by a 300s `select.select` timeout), extracts `; `-joined cookies per origin via `context.cookies(url)`, upserts `SHOPEE_TH_SESSION_COOKIE`/`SHOPEE_TH_AFFILIATE_COOKIE`/`SHOPEE_TH_AFFILIATE_ID` into `.env` in place. `make refresh-cookie` now runs `playwright install chromium` then the script (replacing the bootstrap-ticket TODO stub). README gained the Troubleshoot section SPEC.md stories 6/33 promised but was missing. 7 new unit tests for the pure helpers (`_cookie_header`, `_upsert_env_vars`) via `pythonpath = ["scripts"]`; 95/95 total. Live-portal acceptance step not run (needs a real account/interactive login). `unblocks` `capture-affiliate-portal-traffic`.
- [Dev runbook + end-to-end smoke test](.wayfinder/tickets/dev-runbook-and-smoke-test.md) — `tests/test_smoke.py` (always-on, mocked: page renders, `/health` OK, `/api/search` OK) + `tests/test_e2e.py` (`@pytest.mark.e2e`, real `uvicorn` subprocess + real `HttpTransport`, skips via `pytest.skip` without `SHOPEE_TH_E2E_COOKIE`, otherwise drives the full search→save→re-save-idempotency→list→caption→outputs→clip-prompt→outputs→delete→list-empty loop). `app`/`client` fixtures moved from `test_api.py` into `conftest.py` so both suites share the boot plumbing. `make smoke`/`make e2e` now run real tests instead of a stub/print; `make test` gained `-m "not e2e"` so a stray live-cookie env var can't make the offline target hit the network. **Found and fixed a real bug while dry-running the subprocess boot**: `models/db.py`'s `create_engine()` crashed on a clean clone because `data/` doesn't exist and aiosqlite won't create it — `make run` would have failed on literally any fresh checkout. Added `_ensure_sqlite_dir()`. Verified clean-clone boot manually (`/`, `/health` both 200 with `data/` and the env file removed). 107/107 + 1 e2e-skip; the live lifecycle assertions themselves are unverified pending a real account. `unblocks` nothing further — this was the last open ticket besides `capture-affiliate-portal-traffic`.

## Not yet specified

In-scope fog — questions we expect, but can't phrase sharply yet:

- **Surface B (affiliate portal)** — exact endpoint URLs, GraphQL/REST shape, response JSON for a typical search. The capture tooling ([`capture-affiliate-portal-traffic`](tickets/capture-affiliate-portal-traffic.md)) is built and wired (`make capture-affiliate-traffic`), but still needs a human to run it against a real logged-in account — `docs/research/affiliate-observed-traffic.json` doesn't exist yet, so this is still open/inference-only.
- Whether the affiliate portal returns commission as a single `commissionRate` percent, or a richer `sellerCommissionRate` + `shopeeCommissionRate` + `commission` triple. Affects the `Item` Pydantic model and saved-items schema.
- Exact rate-limit numbers (anecdotal ≤1 req/s with backoff on `error: 90309999` is the working assumption).
- Login-time captcha: presence and form. Surfaces empirically from `implement-cookie-refresh-helper`.
- How the caption template should gracefully handle empty product titles or brand-less items.
- Cookie-expiry symptoms (HTTP status / body shape) and how to surface them in the UI.
- UX detail of "save": saved items in a separate tab/section, or inline with search results?

## Out of scope

Ruled out for this effort (redraw the destination before re-opening these):

- **Real LLM-backed caption / clip-prompt generation** — deferred to a follow-up map. This map ships templated stubs only.
- **Pagination UI and sort selector** on search results — deferred (default = first page, default relevance per J1).
- **Multi-account / multi-locale** support.
- **Auto-refresh** of saved items' price/sold data.
- **Cloud / public deploy** — localhost-only by design (per D1).
- **Mobile-API reverse-engineering** as a first-class surface (assumed; only widen if research can't land an answer).
- **Shopee Open API** — explicitly excluded by the destination itself.
