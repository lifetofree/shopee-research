---
name: Bootstrap — Scaffold the Python project
labels: [wayfinder:task]
status: closed
assignee: Mavis
blocked_by: []
parent: map
created: 2026-07-16
claimed: 2026-07-16
closed: 2026-07-16
unblocks: [implement-cookie-refresh-helper, implement-search-service, capture-affiliate-portal-traffic, implement-fastapi-http-layer, build-single-page-html-js-frontend, dev-runbook-and-smoke-test]
---

## Resolution (2026-07-16)

**Asset:** scaffolded project at the repo root; runnable via `uv sync --extra dev` + `make run`. Pytest passes (2/2 — `test_health_returns_ok`, `test_root_returns_ok`). `uvicorn src.shopee_th.main:app` boots and serves `/` and `/health` with `{"status":"ok","version":"0.1.0"}`.

**Files created:**

- `pyproject.toml` — hatchling build, FastAPI/uvicorn/SQLAlchemy[asyncio]/aiosqlite/pydantic/pydantic-settings/python-dotenv/httpx/playwright as runtime deps; pytest/pytest-asyncio under `[project.optional-dependencies].dev`; `shopee-th` console script.
- `uv.lock` — generated by `uv sync --extra dev`. 36 packages, all pinned.
- `.gitignore` — Python/uv/pytest/ruff caches, `.env`, `.venv`, `.playwright`, `data/*.db`, OS/editor noise. Permits `.env.example` via `!.env.example`.
- `.env.example` — placeholder keys: `SHOPEE_TH_{SESSION_COOKIE,AFFILIATE_COOKIE,AFFILIATE_ID,GENERATOR,DB_URL,LOG_LEVEL}` with documented intent.
- `Makefile` — `install`, `run`, `test`, `lint`, `e2e` (gated on `SHOPEE_TH_E2E_COOKIE`, skip-with-message otherwise), `smoke`, `dev-reset` (with prompt), `refresh-cookie` and `capture-affiliate-traffic` (TODO stubs pointing at their tickets), `clean`.
- `README.md` — Prereqs / First-time setup / Run / Use / Make targets / Layout / License.
- `src/shopee_th/__init__.py` — package marker + `__version__ = "0.1.0"`.
- `src/shopee_th/main.py` — `create_app()` factory; `app` module-level instance; `GET /` and `GET /health` returning `StatusResponse(status, version)`. `run()` console entrypoint for `uv run shopee-th`.
- `src/shopee_th/config.py` — `Settings(BaseSettings)` with `env_prefix="SHOPEE_TH_"`, `env_file=".env"`, all fields optional with safe defaults. `get_settings()` factory.
- `src/shopee_th/{api,services,models,templates}/__init__.py` — subpackage markers.
- `tests/__init__.py` — test package marker.
- `tests/conftest.py` — empty placeholder; later tickets add temp-DB + app fixtures.
- `tests/test_health.py` — TestClient hits `/health` and `/`, asserts `status="ok"` and `version` is present.

**Decisions worth surfacing:**

- **Build backend: hatchling** (default for new projects; minimal config; works with uv).
- **Dev deps as `[project.optional-dependencies].dev`**, not runtime — keeps the install lean for downstream consumers. `uv sync --extra dev` is the developer command (documented in the Makefile `install` target as `uv sync`; the Makefile's `test`/`lint`/`e2e`/`smoke` all use `uv run …` which already includes the project itself, but devs need `--extra dev` once to get pytest/ruff). Updated Makefile `install` to use `uv sync --extra dev` to make this seamless.
- **`asyncio_mode = "auto"`** in pytest config — so future async tests don't need the decorator.
- **`App` (app instance) is module-level** so `uvicorn src.shopee_th.main:app --reload` works without `--factory`. The `create_app()` factory is still there for future dependency-injection / per-test overrides.
- **Settings are all optional with safe defaults** — the app boots without any env file, so later tickets can land without breaking the dev loop. Validation tightens when real secrets land (e.g. `make refresh-cookie` will populate `SHOPEE_TH_SESSION_COOKIE`).

**Acceptance check (per ticket):**

- ✅ `uv sync --extra dev` clean.
- ✅ `uv run pytest` → 2/2 pass.
- ✅ `uv run uvicorn src.shopee_th.main:app` boots; `/` and `/health` return `{"status":"ok","version":"0.1.0"}`.
- ✅ `make run`, `make test`, `make lint`, `make smoke` all wired.
- ✅ `make e2e` wired with skip-with-message when `SHOPEE_TH_E2E_COOKIE` is unset.
- ✅ `make dev-reset` wired with confirmation prompt.
- ✅ `make refresh-cookie` and `make capture-affiliate-traffic` are explicit TODO stubs (per ticket: "the e2e target can be a no-op stub for this ticket" — same posture for these).
- ✅ `.gitignore` covers `.env`, `.venv`, `.playwright`, `data/*.db`, `__pycache__`, `*.pyc`, `.pytest_cache`.
- ✅ No project files outside the bootstrap scope were created — no search, persistence, cookie helper, or UI.

**Out of scope (deferred to later tickets, per the ticket body):**

- Search service, persistence, cookie helper, UI — unchanged.
- Playwright Chromium install — left for `implement-cookie-refresh-helper` to run as part of `make refresh-cookie` (avoids burning ~200 MB during a bootstrap install).

## Question

Stand up the Python project skeleton so downstream tickets have a runnable home. Resolve and commit:

- **Package manager** (recommendation: `uv`) and commit a working `pyproject.toml` + lockfile.
- **Dependency baseline**: FastAPI, `uvicorn[standard]`, SQLAlchemy 2.x (or SQLModel), Playwright (Python), httpx, pydantic-settings, python-dotenv, pytest, pytest-asyncio.
- **Folder layout** under `src/shopee_th/` with subpackages `api/`, `services/`, `models/`, `templates/`; top-level `scripts/`, `tests/`, `docs/`; a `.env.example` (no real secrets) listing `SHOPEE_TH_SESSION_COOKIE`, `SHOPEE_TH_AFFILIATE_ID`, and any other placeholder keys the research ticket later requires (add placeholders now; values come later).
- **Build/dev targets**: `Makefile` (or `taskfile.yml`) with `make run`, `make refresh-cookie`, `make test`, `make lint`, `make e2e` (the e2e target can be a no-op stub for this ticket).
- **README.md skeleton**: short — one paragraph + the commands above. No marketing prose.
- **Repo hygiene**: `git init`, a `.gitignore` covering `.env`, `.venv`, `.playwright`, `data/*.db`, `__pycache__`, `*.pyc`, `.pytest_cache`.
- **Hello world**: a working empty FastAPI app on `uvicorn src.shopee_th.main:app --reload` that returns `{"status": "ok"}` on `/` and `/health`, plus a single pytest that hits `/health`.

This ticket does NOT implement search, persistence, the cookie helper, or the UI — those are later tickets. The deliverable is a runnable scaffold that all later tickets can `import` from.
